export const MaxSimilarCards = 4;

export const DefaultGenre = `All genres`;

export const nameTab = `Overview`;

export const VIDEO_DELAY = 1000;

export const FILMS_LENGTH = 8;

export const сlockClculations = (time) => Math.floor(time / 60 / 60);

export const сalculatingMinutes = (time, hours) => Math.floor(time / 60) - (hours * 60);

export const сalculatingSeconds = (time) => time % 60;
